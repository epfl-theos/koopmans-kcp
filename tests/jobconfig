# Note: unless a specific oreder of execution is desired,
# there is no need to have for each set of tests a field below
[kcp_ozone/]
program = KCP
inputs_args = ('ozone.dft.in'), ('ozone.pz.in'), ('ozone.ki.in')

[bin2xml/]
program = BIN2XML
input_args = ('charge-density.dat'), ('real_space_orb_density.occ.0.00001.dat')

[categories]
kcp_all = kcp_*??
bin2xml_all = bin2xml*
